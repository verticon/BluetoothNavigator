Create a text file viewer.

Create a slide in, side bar menu to access the log file, help file, etc.

Add a notifier to inform the user when the app is in the background. Consider notifications, sounds, app icon badges

Look into connection attempt timeouts. I tried to connet to an iPhone and the state remained in "connecting" for a long time.

If the Characteristic view is on screen and a popuup (such as the Advertising Data) is being displayed and the connection is broken
causing the Charateristic view to be popped then the popup remains on the screen.

